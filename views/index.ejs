<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora Posts</title>
    <!-- Corrected link to the stylesheet inside the public/css folder -->
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div class="container">
        <h1 class="page-title">Quora Feed</h1>

        <% for(let post of posts) { %>
            <div class="post">
                <h3 class="post-username">
                    <%= post.username %>
                </h3>
                <p class="post-content" title="<%= post.content %>">
                    <%= post.content %>
                </p>
                <br>
                <div class="post-controls">
                    <a href="/posts/<%=post.id%>/edit" class="edit-link">Edit</a><br>
                    <form action="/posts/<%=post.id%>?_method=DELETE" method="post" class="delete-form">
                        <button type="submit" class="delete-btn">Delete post</button>
                    </form>
                    <a href="/posts/<%=post.id%>" class="action-btn detail-btn">See in detail</a>
                </div>
            </div>
            <% } %>
                <div class="add-post-container">
                    <a href="/posts/new" class="action-btn">Create a New Post</a>
                </div>
    </div>
</body>

</html>